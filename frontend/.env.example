# =============================================================================
# STANDALONE DEPLOYMENT (Self-Hosted Docker) - DEFAULT CONFIGURATION
# =============================================================================
# This is the configuration for users self-hosting via docker-compose.
# Deployment mode is AUTO-DETECTED - no manual configuration needed.
#
# For maintainer Appwrite Cloud deployment configuration, see:
# docs/APPWRITE_SITES_DEPLOYMENT.md

# Backend URL (Docker internal network)
BACKEND_URL=http://backend

# Feature Flags (optional - defaults provided)
ENABLE_WEB_BLUETOOTH=true
ENABLE_BLE_PROXY=true
ENABLE_COMMUNITY_FEATURES=false

# Development
NODE_ENV=development
